{
  "name": "behider",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "workspaces": [
    "src/webapi",
    "src/webapp"
  ],
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd src && cd webapi && npm install  && cd ../webapp && npm install && npm run build",
    "start": "npm start -w project-behider-webapi"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sucrase": "^3.28.0"
  }
}
