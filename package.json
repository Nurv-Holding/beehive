{
  "name": "behider",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "workspaces": [
    "src/webapi",
    "src/webapp"
  ],
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd src && cd webapi && npm install  && cd ../webapp && npm install && npm run build",
    "start": "node src/webapi/src/index.js && node src/webapp/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sucrase": "^3.28.0"
  }
}
